# Ý tưởng Research: Test-Time Adaptation cho MergeSlide bằng cách cập nhật tham số selected của merged model

> **Tên đề xuất:** **MergeSlide-TTA** — Entropy-Guided Selective Parameter Adaptation for Domain-Shifted WSI Inference  
> **Trạng thái:** Draft v1.0  
> **Dựa trên:** Phân tích trực tiếp từ MergeSlide paper + các bài báo TENT, SHOT, TPT, EATA, SHOT++ trong project

---

## 1. Tóm tắt ý tưởng

MergeSlide sau khi hoàn thành quá trình continual model merging tạo ra bộ tham số hợp nhất $\tilde{\theta}_{1:T}$, cùng với các class-aware prompt embeddings $\{E^{\mathcal{C},t}\}_{t=1}^T$ và task-level prompt embeddings $\mathcal{E}^T$. Khi gặp test WSI từ domain mới (OOD, cross-site domain shift), cơ chế TCP inference có thể suy giảm do phân phối slide embedding $Z_i = f_A(B'_i, \tilde{\theta}_{1:T})$ bị lệch so với phân phối tại thời điểm merging.

**Hướng đề xuất:** Freeze toàn bộ merging coefficients $\lambda_{1:T}$ và prompt embeddings $\{E^{\mathcal{C},t}\}, \mathcal{E}^T$. Chỉ cập nhật **tham số affine của Layer Normalization** trong slide aggregator $f_A$, kết hợp cơ chế **confidence-filtered entropy minimization** ở cấp class và **diversity-regularized entropy** ở cấp task. Không cần source data, không vi phạm cấu trúc merging.

---

## 2. Kiến thức nền liên quan từ các bài báo TTA

### 2.1. Shannon Entropy Minimization — TENT (Wang et al., ICLR 2021)
TENT chỉ cập nhật tham số affine (scale $\gamma$, shift $\beta$) của Batch Normalization theo objective:

$$\mathcal{L}_{\text{TENT}} = -\sum_{c \in \mathcal{C}} f_\theta(c|x) \log f_\theta(c|x)$$

TENT không cần source data, hoạt động online. Hạn chế: dễ collapse khi batch nhỏ hoặc khi dữ liệu mất cân bằng. **Điểm kế thừa:** cập nhật normalization parameters là chiến lược nhẹ nhất, trực tiếp ứng với Layer Norm của transformer trong MergeSlide.

### 2.2. Information Maximization — SHOT (Liang et al., TPAMI 2021)
SHOT freeze classifier (source hypothesis) và chỉ update feature extractor qua loss ghép hai thành phần:

$$\mathcal{L}_{\text{IM}} = \mathcal{L}_{\text{ent}} + \beta \mathcal{L}_{\text{div}}$$

$$\mathcal{L}_{\text{ent}} = -\mathbb{E}_{x \in \mathcal{X}_t}\left[\sum_k \delta_k(f(x)) \log \delta_k(f(x))\right]$$

$$\mathcal{L}_{\text{div}} = D_{\text{KL}}\left(\hat{p} \,\Big\|\, \frac{1}{K}\mathbf{1}_K\right) - \log K, \quad \hat{p} = \mathbb{E}_{x}[\delta(f(x))]$$

$\mathcal{L}_{\text{div}}$ ngăn hiện tượng collapse (tất cả mẫu dự đoán vào một class). **Điểm kế thừa:** trong MergeSlide, prompt embeddings đóng vai trò "source hypothesis" cố định — tương tự cơ chế freeze classifier của SHOT.

### 2.3. Confidence Filtering — EATA (Niu et al., ICML 2022)
EATA thêm sample-adaptive weight $S(x)$ vào entropy loss:

$$S^{\text{ent}}(x) = \frac{1}{\exp[E(x;\Theta) - E_0]} \cdot \mathbb{I}_{\{E(x;\Theta) < E_0\}}(x)$$

Chỉ backward trên mẫu **đáng tin** (entropy thấp) và **không trùng lặp** (cosine diversity). Đồng thời dùng **Fisher regularization** chống catastrophic forgetting. **Điểm kế thừa:** Với WSI, mỗi slide chứa hàng nghìn patch — cần lọc patch đáng tin trước khi tính loss để tránh gradient nhiễu.

### 2.4. Test-Time Prompt Tuning — TPT (Shu et al., NeurIPS 2022)
TPT tối ưu learnable text prompt tokens tại test time qua marginal entropy trên nhiều augmented views, giữ nguyên toàn bộ backbone VLM:

$$p^* = \arg\min_p H\left(\tilde{p}_p(y|X)\right), \quad \tilde{p}_p = \frac{1}{N}\sum_{n=1}^{N} p_p(y|\mathcal{A}_n(X))$$

Kết quả ablation của TPT chứng minh: **text prompt là parameter group hiệu quả nhất**, tốt hơn cả fine-tune full encoder, vì không làm méo pre-trained features. **Điểm kế thừa:** gợi ý rằng trong MergeSlide, nếu cần thêm một nhánh adaptation, learnable prompt offset nhỏ (không thay thế $E^{\mathcal{C},t}$ mà chỉ thêm delta) là hướng an toàn.

### 2.5. Anti-Forgetting Regularization — EATA (Fisher Regularizer)
EATA dùng Fisher information để đo mức độ quan trọng của từng weight $\omega(\theta_i)$, sau đó thêm regularizer:

$$\mathcal{R}(\tilde{\Theta}, \tilde{\Theta}^o) = \sum_{\theta_i \in \tilde{\Theta}} \omega(\theta_i)(\theta_i - \theta_i^o)^2$$

**Điểm kế thừa:** MergeSlide đã có Condition b đảm bảo $\|\tilde{\theta}_{1:t} - \theta_{\text{base}}\|_2$ bị chặn trên. Fisher regularizer tại test time là cơ chế analog để duy trì ràng buộc tương tự trong quá trình TTA.

---

## 3. Bảng đối chiếu nguồn gốc ý tưởng

| Thành phần ý tưởng                          | Bài báo gốc liên quan                   | Phương pháp của bài báo gốc                                             | Cách chuyển sang MergeSlide                                                                                                     | Lý do phù hợp                                                                                             | Rủi ro                                                                                                  |
| ------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Freeze merging coefficients $\lambda_{1:T}$ | SHOT (Liang et al., TPAMI 2021)         | Freeze source classifier (hypothesis), chỉ update feature extractor     | Giữ nguyên $\lambda_{1:T}$ (merging structure) và prompt embeddings; chỉ update LN params                                       | Lambda được thiết kế để duy trì $\|\tilde{\theta} - \theta_{\text{base}}\|_2$; update sẽ phá cấu trúc này | LN params có thể không đủ expressive để bù domain shift lớn                                             |
| Update Layer Normalization affine params    | TENT (Wang et al., ICLR 2021)           | Update BN affine params (scale $\gamma$, shift $\beta$) via entropy min | Thay BN bằng LN (TITAN/Transformer dùng LN), cập nhật $\gamma^l, \beta^l$ cho mỗi lớp $l$ của $f_A$                             | MergeSlide dùng Transformer aggregator (LN, không phải BN); TENT principle trực tiếp áp dụng              | Transformer có nhiều LN layers; cần xác định layer nào update                                           |
| Class-level entropy minimization            | TENT, SHOT                              | $\mathcal{L}_{\text{ent}} = -\sum_c p_c \log p_c$ trên class prediction | Tính entropy trên dot-product similarity score giữa $Z_i$ và $\{E^{\mathcal{C},t}\}$                                            | Class-aware prompts đã fixed; entropy trên similarity score có ý nghĩa trực tiếp                          | Nếu nhiều task có class giống nhau (NSCLC/ESCA đều có adenocarcinoma), entropy có thể bị lẫn            |
| Task-level diversity regularization         | SHOT ($\mathcal{L}_{\text{div}}$)       | KL divergence giữa mean prediction và uniform distribution              | Tính $\mathcal{L}_{\text{div}}$ trên task prediction score $\{Z_i \cdot (E^{\mathcal{T},t})^\top\}$ để tránh collapse về 1 task | TCP inference phụ thuộc vào task prediction chính xác; diversity loss ngăn bias về task phổ biến          | Task distribution trong TCGA không đều (NSCLC phổ biến hơn TGCT); có thể cần class-weighted version     |
| Confidence filtering trên patches           | EATA ($S^{\text{ent}}(x)$), FS-TTA      | Lọc sample có entropy thấp, loại sample entropy cao                     | Lọc top-$K$ patches của WSI theo entropy; chỉ backward trên patches đáng tin                                                    | WSI có hàng nghìn patches, nhiều là background/normal tissue; gradient từ noisy patches phá adaptation    | Cần xác định ngưỡng $E_0$ cho WSI domain (khác với natural image domain)                                |
| Anti-forgetting regularization              | EATA (Fisher), EWC (Kirkpatrick et al.) | Fisher regularizer $\omega(\theta_i)(\theta_i - \theta_i^o)^2$          | Regularize LN params về $\tilde{\theta}_{1:T}$ (giá trị trước TTA), dùng pseudo-Fisher estimate                                 | Duy trì Condition b của MergeSlide: $\|\tilde{\theta}_{1:t} - \theta_{\text{base}}\|_2$ bị chặn           | Fisher estimate từ unlabeled test data có thể không chính xác; cần proxy label từ confident predictions |

---

## 4. Giả thuyết nghiên cứu

> **Giả thuyết chính:** Khi MergeSlide gặp domain shift (cross-site OOD), sự suy giảm hiệu năng chủ yếu đến từ **sự lệch phân phối của slide embeddings $Z_i$** so với các prompt embeddings $\{E^{\mathcal{C},t}\}$ — không phải từ sai lệch trong merging coefficients $\lambda_{1:T}$. Do đó, cập nhật nhẹ các tham số affine normalization trong $f_A$ đủ để căn chỉnh lại phân phối embedding, trong khi giữ nguyên cấu trúc merging bảo toàn tri thức đa nhiệm.

**Kiểm chứng được khi:** Mô hình sau TTA có bACC cao hơn MergeSlide gốc trên OOD setting (cross-site), trong khi không suy giảm trên in-domain test set.

---

## 5. Phương pháp đề xuất: MergeSlide-TTA

### Tên: MergeSlide-TTA: Selective Normalization Adaptation with Dual-Level Entropy Guidance

### Thành phần Frozen:
- Merging coefficients $\lambda_{1:T}$ và merged weights $\tilde{\theta}_{1:T}$ (trừ LN affine params)
- Text encoder của TITAN
- Class-aware prompt embeddings $\{E^{\mathcal{C},t}\}_{t=1}^T$
- Task-level prompt embeddings $\mathcal{E}^T$

### Thành phần được Update:
- Tham số affine của **Layer Normalization** trong $f_A$: $\{\gamma^l, \beta^l\}_{l=1}^L$ (chỉ scale và shift, không update attention weights hay FFN weights)

### Lý do không update lambda:
$\lambda_t$ trong MergeSlide được thiết kế theo Eq. (5) để đảm bảo $\|\tilde{\theta}_{1:t} - \theta_{\text{base}}\|_2 \leq \max_i \|\theta_i\|_2$. Thay đổi $\lambda_t$ tại test time sẽ phá vỡ ràng buộc này, làm mất tính chất "không drift quá xa base model" — đây là bảo đảm chống catastrophic forgetting cốt lõi của MergeSlide.

### Lý do update LN params hợp lý hơn:
Layer Normalization chỉ có $2d$ tham số (với $d$ là hidden dim), không thay đổi kiến trúc. Các tham số $\gamma, \beta$ quyết định **phân phối feature** tại mỗi lớp nhưng không ảnh hưởng đến **hướng** của merged task vectors $\Delta\tilde{\theta}_{1:t}$. Tương tự như TENT đã chứng minh: BN/LN affine params là parameter group tối ưu cho adaptation — đủ expressive nhưng không gây catastrophic forgetting.

### Đảm bảo không cần source data:
Tất cả gradients chỉ dựa trên dự đoán của model trên test WSI hiện tại. Không cần lưu trữ hay truy cập WSI training. Thông tin duy nhất cần giữ lại: (1) frozen model weights $\tilde{\theta}_{1:T}$, (2) prompt embeddings, (3) giá trị LN params ban đầu $\{\gamma^l_0, \beta^l_0\}$ để regularize.

---

## 6. Thiết kế loss
[[01.1. Giải thích ý nghĩa các hàm loss]]
Loss tổng quát:

$$\mathcal{L}_{\text{TTA}} = \mathcal{L}_{\text{class}} + \alpha \mathcal{L}_{\text{task}} + \beta \mathcal{R}_{\text{forget}}$$

### 6.1. Class-level adaptation loss $\mathcal{L}_{\text{class}}$

Lấy cảm hứng từ **SHOT** (Information Maximization) và **EATA** (confidence filtering):

$$\mathcal{L}_{\text{class}} = \underbrace{-\sum_{c=1}^{C} \tilde{p}_c \log \tilde{p}_c}_{\text{entropy min (TENT/SHOT)}} - \underbrace{\sum_{c=1}^{C} \bar{p}_c \log \bar{p}_c}_{\text{diversity (SHOT } \mathcal{L}_{\text{div}}\text{)}}$$

Trong đó:
- $\tilde{p}_c = \text{softmax}(Z_i \cdot (E^{\mathcal{C},\hat{t}})^\top)_c$: xác suất class $c$ sau khi TCP xác định task $\hat{t}$
- $\bar{p}_c = \frac{1}{|\mathcal{B}_K|}\sum_{i \in \mathcal{B}_K} \tilde{p}_c^{(i)}$: mean prediction trên batch các patch **đã được lọc** $\mathcal{B}_K$
- $\mathcal{B}_K$: tập $K$ patches có entropy thấp nhất (top-$K\%$ confident patches, theo EATA/FS-TTA):

$$\mathcal{B}_K = \left\{i : H(\tilde{p}^{(i)}) < E_0\right\}, \quad E_0 = \text{threshold}$$

**Vai trò từng thành phần:**
- Entropy minimization: ép model đưa ra dự đoán class chắc chắn hơn trên target domain.
- Diversity term (âm của marginal entropy): ngăn collapse về một class, đặc biệt quan trọng với ung thư mất cân bằng (ví dụ NSCLC vs. TGCT).

### 6.2. Task-level adaptation loss $\mathcal{L}_{\text{task}}$

Lấy cảm hứng từ **TPT** (marginal entropy trên multiple views) kết hợp **SHOT** ($\mathcal{L}_{\text{div}}$):

$$\mathcal{L}_{\text{task}} = -\sum_{t=1}^{T} q_t \log q_t - \lambda_{\text{div}} \cdot \sum_{t=1}^{T} \bar{q}_t \log \bar{q}_t$$

Trong đó:
- $q_t = \text{softmax}(Z_i \cdot (E^{\mathcal{T},t})^\top)_t$: xác suất task $t$ từ TCP scoring
- $\bar{q}_t = \frac{1}{|\mathcal{B}_K|}\sum_{i \in \mathcal{B}_K} q_t^{(i)}$: mean task prediction trên batch đã lọc
- $\lambda_{\text{div}}$: trọng số diversity term cho task level

**Vai trò:** Task prediction accuracy là nút thắt quan trọng của TCP inference. Nếu model bị domain shift khiến task prediction sai, class prediction sau đó cũng sai. Tối ưu $\mathcal{L}_{\text{task}}$ trực tiếp sửa alignment giữa slide embedding và task-level prompt.

### 6.3. Anti-forgetting regularizer $\mathcal{R}_{\text{forget}}$

Lấy cảm hứng từ **EATA** (Fisher regularizer):

$$\mathcal{R}_{\text{forget}} = \sum_{l=1}^{L} \left[\omega_\gamma^l (\gamma^l - \gamma^l_0)^2 + \omega_\beta^l (\beta^l - \beta^l_0)^2\right]$$

Trong đó:
- $\gamma^l_0, \beta^l_0$: giá trị LN params trước khi TTA (từ merged model $\tilde{\theta}_{1:T}$)
- $\omega_\gamma^l, \omega_\beta^l$: pseudo-Fisher importance — ước lượng từ confident pseudo-labels trên test batch

$$\omega_\gamma^l = \frac{1}{|\mathcal{B}_K|} \sum_{i \in \mathcal{B}_K} \left(\frac{\partial \mathcal{L}_{\text{CE}}(f(x_i), \hat{y}_i)}{\partial \gamma^l}\right)^2$$

với $\hat{y}_i = \arg\max \tilde{p}^{(i)}$ là pseudo-label từ confident patches.

---

## 7. Thuật toán test-time adaptation

```
Input:  Test WSI X_i
        Merged model f_A(·, θ̃_{1:T}) với θ̃ gồm frozen weights + LN params {γ^l, β^l}
        Prompt embeddings {E^{C,t}}, E^T (frozen)
        Hyperparams: K%, E_0, α, β, lr, n_steps

Output: Prediction ŷ_i

--- BƯỚC 1: FORWARD PASS ---
1.1. Sample K=400 patches B'_i từ WSI X_i (giống MergeSlide training)
1.2. Extract patch embeddings {v_j} qua TITAN vision encoder (frozen)
1.3. Forward qua f_A: Z_i = f_A(B'_i, θ̃_{1:T})  [LN params trainable ở đây]

--- BƯỚC 2: DUAL-LEVEL PREDICTION ---
2.1. Task scores: s_t = Z_i · (E^{T,t})^T  cho mọi t
     q = softmax({s_t})  → task probability distribution
     t̂ = argmax q        → predicted task (TCP)

2.2. Class scores: r_c = Z_i · (e^{C,t̂}_c)^T  cho mọi c trong task t̂
     p̃ = softmax({r_c})  → class probability distribution

--- BƯỚC 3: CONFIDENCE FILTERING ---
3.1. Tính per-patch entropy: H^(j) = -Σ_c p̃^(j)_c log p̃^(j)_c
3.2. Lọc: B_K = {j : H^(j) < E_0}  (top-K% low-entropy patches)
3.3. Nếu |B_K| < min_batch: skip update, return prediction trực tiếp

--- BƯỚC 4: TÍNH LOSS ---
4.1. Class-level entropy loss:
     L_ent = -Σ_c p̃_c log p̃_c  (trung bình trên B_K)
     p̄_c = mean_{j∈B_K}(p̃^(j)_c)
     L_div_class = -Σ_c p̄_c log p̄_c  (marginal entropy)
     L_class = L_ent - L_div_class

4.2. Task-level loss:
     L_task_ent = -Σ_t q_t log q_t
     q̄_t = mean_{j∈B_K}(q^(j)_t)
     L_task = L_task_ent - λ_div · (-Σ_t q̄_t log q̄_t)

4.3. Anti-forgetting regularizer:
     Compute pseudo-Fisher ω^l từ confident pseudo-labels (B_K)
     R_forget = Σ_l [ω^l_γ (γ^l - γ^l_0)^2 + ω^l_β (β^l - β^l_0)^2]

4.4. Total loss:
     L_TTA = L_class + α · L_task + β · R_forget

--- BƯỚC 5: UPDATE LN PARAMS ---
5.1. Gradient: ∇_{γ^l, β^l} L_TTA
5.2. Update: {γ^l, β^l} ← {γ^l, β^l} - lr · gradient  (n_steps bước)

--- BƯỚC 6: INFERENCE ---
6.1. Re-forward với updated LN params
6.2. Tính lại Z_i, q, t̂, p̃
6.3. ŷ_i = argmax p̃

--- BƯỚC 7: RESET POLICY ---
Option A (Episodic): Reset {γ^l, β^l} về {γ^l_0, β^l_0} sau mỗi slide
Option B (Continual): Giữ nguyên params đã adapted cho slide tiếp theo
→ Cần ablate cả hai
```

---

## 8. Kế hoạch coding experiment

### Môi trường:
- Framework: PyTorch, dựa trên codebase gốc `caodoanh2001/MergeSlide`
- GPU: RTX 4090 hoặc RTX 8000 (giống paper gốc)
- Dataset: TCGA 6 tasks (BRCA, RCC, NSCLC, ESCA, TGCT, CESC) — OOD setting (cross-site)

### Các bước implement:

**Bước 1:** Clone MergeSlide codebase, load pretrained merged weights $\tilde{\theta}_{1:T}$.

**Bước 2:** Viết hàm `get_ln_params(model)` — trích xuất và set `requires_grad=True` chỉ cho LN affine params, freeze tất cả còn lại.

**Bước 3:** Implement `patch_confidence_filter(patch_embeddings, E_0)` — tính per-patch entropy và filter.

**Bước 4:** Implement `compute_tta_loss(Z_i, class_prompts, task_prompts, B_K, alpha, beta)` theo công thức Section 6.

**Bước 5:** Implement `compute_pseudo_fisher(model, B_K, pseudo_labels)` — tính $\omega(\theta_i)$ one-shot.

**Bước 6:** Wrap thành `MergeSlide_TTA.adapt(slide_batch, n_steps=1)` — hỗ trợ cả episodic và continual mode.

**Bước 7:** Evaluate trên OOD test set, ghi lại bACC, ACC, BWT, FGT.

---

## 9. Baseline và ablation study

### Baselines cần so sánh:
| Baseline                                                      | Lý do                                                                      |
| ------------------------------------------------------------- | -------------------------------------------------------------------------- |
| MergeSlide (no TTA)                                           | Upper bound reference                                                      |
| MergeSlide + TENT trên LN                                     | So sánh: chỉ class entropy, không có task entropy hay diversity            |
| MergeSlide + SHOT-IM trên LN                                  | So sánh: information maximization nhưng không có task-level loss           |
| MergeSlide + EATA trên LN                                     | So sánh: confidence filtering + anti-forgetting, nhưng không có dual-level |
| MergeSlide-TTA (ours, không có $\mathcal{L}_{\text{task}}$)   | Ablation: chỉ class-level                                                  |
| MergeSlide-TTA (ours, không có $\mathcal{R}_{\text{forget}}$) | Ablation: không có regularizer                                             |
| MergeSlide-TTA Full                                           | Proposed method                                                            |

### Ablation study:
1. **Ablation thành phần loss:** Loại bỏ từng trong $\{\mathcal{L}_{\text{class}}, \mathcal{L}_{\text{task}}, \mathcal{R}_{\text{forget}}\}$.
2. **Ablation parameter group:** So sánh update LN vs. update full $f_A$ vs. update attention layers only.
3. **Ablation confidence threshold $E_0$:** Grid search $\{0.3, 0.5, 0.7, 0.9\} \times \log C$.
4. **Ablation reset policy:** Episodic vs. continual.
5. **Ablation số bước update:** $n_{\text{steps}} \in \{1, 3, 5\}$.

### Metrics đánh giá:
- **bACC** (Balanced Accuracy) — metric chính của MergeSlide paper
- **ACC** (Overall Accuracy)
- **BWT** (Backward Transfer) — đo forgetting sau TTA
- **$\Delta\text{bACC}_{\text{out-in}}$** — chênh lệch OOD vs. IND (metric domain robustness của MergeSlide)
- **Task prediction accuracy** — accuracy của TCP bước $\hat{t}$ trước khi predict class
- **Inference time** — overhead so với MergeSlide gốc (slides/s)

### Expected outcome:
- MergeSlide-TTA Full > MergeSlide (no TTA) trên OOD setting (+1–3% bACC).
- Task-level loss có đóng góp rõ ràng (ablation: -$\mathcal{L}_{\text{task}}$ giảm performance).
- Regularizer $\mathcal{R}_{\text{forget}}$ quan trọng trong continual mode (ngăn drift qua nhiều slides).

---

## 10. Rủi ro và cách kiểm chứng

| Rủi ro | Cách kiểm chứng |
|---|---|
| LN params quá ít tham số, không đủ để bù domain shift | So sánh với "update attention layers" baseline; nếu gap lớn thì mở rộng scope |
| Diversity loss không cân bằng được class imbalance của TCGA | Test riêng trên TGCT (rare task) và NSCLC (dominant task); dùng Tsallis entropy thay thế nếu cần |
| Task prediction sai (TCP error) làm loss tính sai class | Log task prediction accuracy riêng; nếu > 10% error thì cần cơ chế fallback |
| Over-adaptation khi nhiều slides liên tiếp (continual mode) | Monitor bACC trên in-domain slides xen kẽ; nếu giảm, dùng episodic mode hoặc tăng $\beta$ |
| Fisher importance estimate không chính xác trên WSI | So sánh với simple $\ell_2$ regularizer (không dùng Fisher); nếu tương đương thì dùng đơn giản hơn |
| Augmentation overhead (nếu dùng marginal entropy như TPT) | Tránh augmentation nhiều views; dùng patch-level confidence filtering thay thế |

---

## 11. Expected contributions

1. **Methodological:** Lần đầu tiên đề xuất test-time adaptation cho model merging trên WSI, với thiết kế dual-level entropy objective (class + task) phù hợp với TCP inference của MergeSlide.

2. **Practical:** Chứng minh rằng chỉ cần cập nhật LN affine params (số lượng nhỏ, $\ll 1\%$ tổng params) là đủ để giảm domain gap của VLM-based WSI classification mà không cần source data.

3. **Theoretical insight:** Bổ sung bằng chứng thực nghiệm rằng merging coefficients $\lambda_t$ nên được frozen trong TTA — không phải vì lý do computational, mà vì chúng encode cấu trúc orthogonal merging quan trọng cho stability.

4. **Benchmark:** Cung cấp OOD TTA benchmark mới trên TCGA 6-task stream, có thể tái sử dụng cho các nghiên cứu sau.

---

## 12. Next steps

1. **Tuần 1–2:** Clone MergeSlide codebase, verify OOD baseline numbers (reproduce Table 4 của paper gốc).
2. **Tuần 3:** Implement MergeSlide-TTA module (LN update + dual-level loss, không có Fisher regularizer trước).
3. **Tuần 4:** Ablation các thành phần loss; xác định hyperparameters $\alpha, \beta, E_0$.
4. **Tuần 5:** Implement Fisher regularizer; so sánh episodic vs. continual mode.
5. **Tuần 6:** Full comparison vs. all baselines; write up results.
6. **Tùy chọn:** Nếu kết quả tốt, thử mở rộng sang **learnable prompt offset** $\delta_p$ (thêm delta nhỏ vào $E^{\mathcal{C},t}$, không thay thế) — hướng bổ sung lấy cảm hứng từ TPT.

---

> **Ghi chú quan trọng:** Bài báo MergeSlide gốc đã có OOD evaluation với $\Delta\text{bACC}_{\text{out-in}} = -2.817\%$. Đây là baseline suy giảm cụ thể để TTA cần cải thiện. Mục tiêu thực tế: giảm gap này về $< 1.5\%$ mà không làm giảm IND performance.
